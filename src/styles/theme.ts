import { Theme } from '@emotion/react'

const theme: Theme = {
  darkMode: true,

  color: {
    primary: '#000',
    positive: '#fff',
    negative: '#000',
    white: '#fff',
    black: '#000'
  }
}

export default theme
